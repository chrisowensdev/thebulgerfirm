import React from "react";
import { HeartCrack, UserCheck, Handshake } from "lucide-react";

const FamilyLaw = () => {
	return (
		<div>
			<HeartCrack size={32} className="text-brand-dark" />
			<UserCheck size={32} className="text-brand-dark" />
			<Handshake size={32} className="text-brand-dark" />
		</div>
	);
};

export default FamilyLaw;
